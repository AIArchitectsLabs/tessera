{
  "$schema": "https://schema.tauri.app/config/2.json",
  "identifier": "main-capability",
  "description": "Grants the main window access to core Tauri APIs. Custom Rust commands (invoke_handler) do not require capability entries.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
